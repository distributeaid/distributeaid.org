---
label: Generic Page
hide_body: true
fields:
- name: title
  type: text
  config:
    required: true
  label: Page Title
  description: 'NOTE: the URL will be generated based on this field (e.g. About Us
    -&gt; distributeaid.org/about-us), so make sure you give this a good, general
    title that isn''t likely to change.'
- name: sections1
  type: list
  config:
    use_select: false
    min: 
    max: 
  label: Content Sections (Sortable List)
- name: sections2
  type: field_group_list
  config:
    min: 
    max: 
    labelField: 
  label: Content Sections (Repeatable Field Group)
- name: sections3
  type: blocks
  label: Content Sections (Blocks)
  template_types:
  - section-grid
  config:
    min: 
    max: 
pages:
- content/pages/our-story.md
