---
label: Fundraiser
hide_body: false
display_field: title
fields:
- name: title
  type: text
  config:
    required: true
    min: 2
  label: Title
  description: The title of the fundraiser.
- name: target
  type: number
  config:
    required: true
    min: 1
  label: Target
  description: The target amount.
- name: raised
  type: number
  config:
    required: true
    min: 0
  label: Raised
  description: The amount already raised.
- name: currency
  type: text
  config:
    required: true
    min: 3
    max: 3
  label: Currency
  description: Possible values are the ISO 4217 currency codes, such as "USD" for
    the US dollar, "EUR" for the euro, or "CNY" for the Chinese RMB — see the Current
    currency & funds code list at https://www.iban.com/currency-codes.
- name: abstract
  type: textarea
  config:
    required: true
    wysiwyg: true
    schema:
      format: markdown
  label: Abstract
  description: This text appears on the overview page
- type: file
  name: hero
  label: Hero
  description: Provide the hero image for the fundraiser
  hidden: false
  config:
    required: true
- type: image_gallery
  name: gallery
  label: Gallery
  description: Provide additional photos for the fundraiser
  config:
    required: true
pages:
- content/blocks/fundraisers/afghanistan-earthquake.md
- content/blocks/fundraisers/europe.md
- content/blocks/fundraisers/ukraine-response.md
