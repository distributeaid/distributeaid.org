---
label: Fundraiser
hide_body: false
display_field: title
fields:
- name: title
  type: text
  config:
    required: true
    min: 2
  label: Title
  description: The title of the fundraiser.
- name: target
  type: number
  config:
    required: true
    min: 1
  label: Target
  description: The target amount.
- name: raised
  type: number
  config:
    required: true
    min: 0
  label: Raised
  description: The amount already raised.
- name: currency
  type: text
  config:
    required: true
    min: 3
    max: 3
  label: Currency
  description: Possible values are the ISO 4217 currency codes, such as "USD" for
    the US dollar, "EUR" for the euro, or "CNY" for the Chinese RMB — see the Current
    currency & funds code list at https://www.iban.com/currency-codes.
- name: abstract
  type: textarea
  config:
    required: true
    wysiwyg: true
    schema:
      format: markdown
  label: Abstract
  description: This text appears on the overview page
- type: field_group_list
  name: gallery
  label: Gallery
  description:  Provide photos for the fundraiser. The first photos is used as the hero image for the fundraiser.
  hidden: false
  fields:
  - type: file
    name: url
    label: URL
    hidden: false
    config:
      required: true
  - name: alt
    type: text
    config:
      required: true
      min: 2
    label: Alternative text
    description: Description of the photo, used as the alternative text.
  config:
    labelField: alt
    required: true
    min: 1
pages:
- content/blocks/fundraisers/european-refugee-response.md
- content/blocks/fundraisers/humanitarian-logistics.md
- content/blocks/fundraisers/sustainable-supply-chains.md
- content/blocks/fundraisers/tech-for-good.md
- content/blocks/fundraisers/ukraine-response-project.md
- content/blocks/fundraisers/us-disaster-relief.md
